[project]
name = "aurelion-cli"
version = "0.1.4"
description = "CLI for Aurelion"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.27.0",
    "rich>=14.3.3",
    "typer>=0.24.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]
